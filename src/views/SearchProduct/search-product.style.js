import { makeStyles } from "@material-ui/core/styles";
export const useSearchProductStyles = makeStyles({
  root: {
    height: "calc(100vh - 96px)",
    position: "relative",
    marginTop: "48px"
  }
});
